steps:
  - name: 'gcr.io/cloud-builders/npm'
    id: 'Install dependencies'
    args: ['install']


  - name: 'gcr.io/cloud-builders/npm'
    id: 'start'
    args: ['start']

  # Build snapshot images and push them to Cloud Registry

  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build deployable image (commit tag)'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/moleculer-microservice:$SHORT_SHA', '.']
    
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push deployable image (commit tag)'
    args: ['push', 'gcr.io/$PROJECT_ID/moleculer-microservice:$SHORT_SHA']
    
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build deployable image (latest tag)'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/moleculer-microservice:latest', '.']
    
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push deployable image (latest tag)'
    args: ['push', 'gcr.io/$PROJECT_ID/moleculer-microservice:latest']
    
images:
  - 'gcr.io/$PROJECT_ID/moleculer-microservice:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/moleculer-microservice:latest'